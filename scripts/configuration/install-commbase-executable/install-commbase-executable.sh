#!/bin/env bash
# install-commbase-executable.sh
# Copies the Commbase executable to the executable binaries directory
# Requires root user permissions to run as root or run with sudo to work

# Copy commbase to /usr/bin
cp commbase.default /usr/bin/commbase

exit 99

