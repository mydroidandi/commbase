echo_lines_from_file_variable() {
	for line in "${control_stop_previous_command_patterns[@]}"; do
		echo "$line"
	done
}

	: '
	for file in "${files[@]}"; do
		echo "Contents of $file:"
		lines=$(read_lines_from_file "$file")
		for line in "${lines[@]}"; do
			echo "$line"
		done
		echo
	done
	'
